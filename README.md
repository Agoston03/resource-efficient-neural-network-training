# resource-efficient-neural-network-training

This repository benchmarks resource-efficient training of a CIFAR‑10 ResNet‑20 by inducing sparsity during training with proximal gradiend operators applied to convolution layers.
